# Rename this to .env and replace with environment specific vals

# Changes to this file only take effect upon server restart

# API url can be a full URL or // to use window.location.href
API_URL=//
TILE_SERVER_URL=http://maps-stage-api.nrel.gov/windview-tiles

# Enable redux-logger = log actions and state
LOG_ACTIONS=true
