import React from 'react';
import './ChartElement.scss';
import { BaseElement } from '../AggregatedForecastChart';

const ChartElement = ({forecast}) => {
  return (
    <BaseElement>
      This is where the aggregation chart will render
    </BaseElement>
  )
}

export default ChartElement;
